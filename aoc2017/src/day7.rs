#![allow(unused_imports)]

use crate::get_data;


pub fn p1(data: String) -> usize {
    let result = 0;

    return result;
}

pub fn p2(data: String) -> usize {
    let result = 0;

    return result;
}

#[test]
fn test_d7() {
    let data = get_data(7);
    assert_eq!(p1(data.clone()), 0);
    assert_eq!(p2(data), 0);
}